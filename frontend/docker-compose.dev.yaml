services:
  frontend:
    image: crypto-frontend:dev
    build: .
    restart: unless-stopped
    volumes:
      - ./src:/app/src
      - ./public:/app/public
    environment:
      - VITE_API_HOST=http://localhost:3000
    ports:
      - "5173:5173"