import React from "react";

import { Tab, TabList, TabPanel, Tabs } from "react-tabs";
{/* <BuyCoin selectedCoin={selectedCoin} /> */ }
const BuyCrypto = ({ selectedCoin }: { selectedCoin: string }) => {
    return (
        <div>
            
        </div>
    );
}

export default BuyCrypto;
